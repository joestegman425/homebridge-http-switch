{
  "pluginAlias": "HomebridgeHttpSwitch",
  "pluginType": "platform",
  "singular": true,
  "strictValidation": false,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "default": "Homebridge Http Switch"
      },
      "webhookPort": {
        "title": "Homebridge webhooks port",
        "type": "integer",
        "default": 51440
      },
      "switches": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "name": {
              "title": "Name",
              "description": "Name of the switch in the Home app",
              "type": "string",
              "required": true,
              "placeholder": "<enter new name>"
            },
            "defaultState": {
              "title": "Sets the default switch state",
              "type": "string",
              "oneOf": [
                { "title": "on", "enum": ["on"] },
                { "title": "off", "enum": ["off"] }
              ]
            },
            "autoToggle": {
              "title": "When toggled off the default state, will auto toggle back to the default state (creates a virtual button)",
              "type": "boolean"
            },
            "autoToggleDelay": {
              "title": "Time to wait before auto toggling (in seconds)",
              "type": "integer"
            },
            "onUrl": {
              "title": "Send this GET request when the switch is turned on",
              "type": "string"
            },
            "onWebhookPath": {
              "title": "Turn the switch on when this web hook is received",
              "type": "string"
            },
            "offUrl": {
              "title": "Send this GET request when the switch is turned off",
              "type": "string"
            },
            "offWebhookPath": {
              "title": "Turn the switch off when this web hook is received",
              "type": "string"
            },
            "statusUrl": {
              "title": "If present, this URL will be used to get the current accessory status",
              "type": "string"             
            },
            "statusOnRegex": {
              "title": "If using a status URL, this is a regular expression used on the response to determine 'on' state",
              "type": "string"             
            }
          }
        }
      }
    }
  }
}